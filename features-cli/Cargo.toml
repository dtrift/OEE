[package]
name = "features-cli"
description = "Single Rust feature code for training (export) and inference: feature parity by construction (weeks 3-4)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# no_std transcendental functions for the feature extractor (sqrt/cos);
# pinned to the workspace lock version (0.2.16) — no resolution change.
libm = "0.2"
