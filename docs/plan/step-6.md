# Неделя 6 — декомпозиция (QEMU + бенчмарки + отчёт + демо)

> Ветка: `feat/qemu-report` (OEE) + `bench/conv1d-vs-conv2d` (fork/microflow)

> Разложение строки «Неделя 6» из плана [`plan.md`](../plan.md),
> разд. 9. Финальная неделя: переносимость (QEMU LM3S6965), числа (criterion,
> flash/RAM), отчёт и записанное демо. Резерва после неё нет — недоделанное режется
> по списку приоритетов (эскалации). Режим: 1 человек; будни ~2–3 ч, суббота
> ~4 ч. Оценка: ~16–19 ч. Вход: полный пайплайн на хосте, таблица measured vs truth.

## Гейт недели (минимально готово)

- [ ] QEMU: пример форка и своя модель на LM3S6965, вывод через UART.
- [ ] Footprint: flash/RAM из ELF; Δ «с Conv1D vs без» и vs reshape-трюк.
- [ ] Бенчмарки criterion: латентность min/avg/max, Conv1D vs Conv2D-трюк.
- [ ] Отчёт собран (results, ограничения); демо записано и воспроизводится.

## Сводка по дням

| День | Тема сессии | Суть                                      | Артефакт                |
| ---- | ----------- | ----------------------------------------- | ----------------------- |
| Д1   | QEMU        | стенд: qemu lm3s6965evb, тулчейн, примеры | пример идёт через UART  |
| Д2   | QEMU        | свой no_std бинарник с моделью A          | predict в UART          |
| Д3   | footprint   | flash/RAM из ELF, сравнения               | таблица footprint       |
| Д4   | бенчмарки   | criterion: Conv1D vs трюк, латентности    | таблица чисел           |
| Д5   | отчёт       | results, ограничения, выводы              | черновик отчёта целиком |
| Д6   | демо        | сценарий записи, запуск одной командой    | видеозапись демо        |
| Д7   | финал       | воспроизводимость с чистого клона, сдача  | всё собрано             |

## Д1 (пн, ~2–3 ч) — QEMU: стенд

1. `qemu-system-arm -M lm3s6965evb`; тулчейн `thumbv7m-none-eabi` (разд. 7).
2. Запуск примеров форка на QEMU, вывод через UART.
3. Зафиксировать версии QEMU/тулчейна и команды — в README/отчёт.

Проверка: пример форка печатает в UART-консоль.

## Д2 (вт, ~3 ч) — QEMU: своя модель

1. Минимальный no_std бинарник: модель A (или логика P) + UART-вывод.
2. Прогон на QEMU; сверка предсказаний с хостом — должны совпасть.
3. Честно: тайминги не cycle-accurate (риск разд. 11) — в ограничения отчёта.

Проверка: QEMU и хост дают одинаковые предсказания на тех же входах.

## Д3 (ср, ~2 ч) — footprint

1. `cargo-size`/из ELF: flash/RAM бинарника с моделью.
2. Сравнения: dense-базлайн без Conv1D vs с Conv1D; vs reshape-трюк Conv2D.
3. Таблица footprint — в раздел «results» отчёта.

## Д4 (чт, ~3 ч) — бенчмарки criterion

1. Criterion на хосте (основной источник чисел, разд. 7): честный кернел
   Conv1D vs Conv2D-через-reshape; латентность min/avg/max моделей A/Q.
2. Несколько прогонов; фиксация чисел таблицей.
3. В отчёт: сырые результаты + 3–5 выводов интерпретации.

## Д5 (пт, ~3–4 ч) — отчёт

1. Структура: задача, архитектура, вклад в движок, эксперименты (OEE-таблица
   недели 5), метрики движка/узлов (confusion matrices), ограничения, future work.
2. Разделы из разд. 10 плана: OEE-корректность, движок, узлы, детерминизм.
3. Прочитать целиком один раз глазами: логика изложения, а не только факты.

## Д6 (сб, ~3–4 ч) — демо

1. Сценарий записи (2–4 мин): стенд в работе, дашборд, micro-stop и брак на
   глазах меняют OEE.
2. Запуск всего одной командой: скрипт недели 5 (mosquitto + симулятор + узлы +
   `cargo run -p oee-dashboard`); терминал на полный экран — запись чистая.
3. Запись + короткий резервный дубль.

## Д7 (вс, ~2 ч) — финальный гейт

1. Воспроизводимость: чистый клон → сборка → прогон → цифры совпали.
2. Чеклист недели; недоделки — в future work отчёта, не в «скоро доделаю».
3. Сдача: всё собрано, демо воспроизводится — финальная строка плана.

Артефакт: `tmp/OEE/week6-gate.md` + отчёт и видео.

## Точки эскалации

- QEMU-часть не идёт → приоритет: бенчмарки и отчёт важнее; QEMU описать честно
  как частичный результат (примеры форка идут, своя модель — нет, и почему).
- Не хватает времени на всё → резать в порядке: детали QEMU → красота дашборда →
  экзотика бенчмарков. НЕ резать: OEE-таблица, метрики движка, демо.
- Отчёт грозит стать «хроникой» → формат: один вывод = одна таблица/график +
  2–3 предложения; хроника — в приложение.

## Анти-скоп (чего НЕ делаем на неделе 6)

- PR в апстрим — после курса (разд. 12); камера и QAT — future work.
- Новые эксперименты — только если в отчёте видна дыра.
- Доработки симулятора «для красоты» — заморозка фич, только багфиксы.
