# Мелочи: qemu

- **Серьёзность**: мелочь
- **Компонент**: qemu
- **Источник**: ревью `backlog/20260909120000-review.md`, приложение А6, «Мелочи»

Сводный список мелких замечаний:

1. `qemu/src/lib.rs:63` — `STATE_NAMES[argmax]` без защиты, при этом `firmware/a` использует `.get(...).unwrap_or("?")`. Сделать одинаково (при NaN `.unwrap()` на L47 упадёт раньше).
2. `qemu/src/windows.rs:5` — doc говорит «included by the firmware (`qemu/src/main.rs`)», на деле модуль подключает `lib.rs` (`pub mod windows`). DocSync-нить.
3. `qemu/Cargo.toml:40-41` — footprint-числа собираются при дефолтном `opt-level = 3`; для size-заявлений честно добавить (или упомянуть в README) вариант `opt-level = "z"` — относительное сравнение ядер это не сломает.
4. `scripts/qemu-parity.sh` — покрытие паритета: 4 окна happy-path (первая строка каждого класса), без регенерационной сверки `windows.rs` ↔ `model_a.val.csv` (drift при переобучении). Добавить тест/шаг, пересчитывающий `gen-qemu-windows.py` в temp и сравнивающий.
5. `qemu/src/uart.rs:32` — busy-wait TX без таймаута: на реальном железе зависание (в QEMU backend всегда_drain'ится) — уже честно задокументировано; при портировании добавить bounded-loop.
