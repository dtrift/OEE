# Рекомендации: mqtt-min

- **Серьёзность**: рекомендация
- **Компонент**: mqtt-min
- **Источник**: ревью `backlog/20260909120000-review.md`, приложение А4, «Рекомендации»

1. **Вынести framing в один приватный модуль** (varint + чтение полного пакета + `MAX_PACKET_SIZE`) и использовать его в клиенте и брокере — закроет `20260909120023-important-no-max-packet-size-mqtt-min-bl.md` и дублирование varint-ридера одним изменением.
2. **Дрейн capture-канала и статистика в `bin/broker.rs`** (периодически: сообщения, клиенты, отключения в stdout) — закрывает утечку (`20260909120024-important-capture-channel-leak-mqtt-min-bl.md`); broker.log в soak сегодня бесполезен, а телеметрия транспорта ценна для анализа бенча.
3. **Переименовать `testing`-модуль**: ядро брокера → `broker`, тестовые хелперы → `testing`. Bench-бинарник не должен импортировать «тестовую инфраструктуру» как прод-код.
4. **Задокументировать контракт `Client`**: отсутствие авто-reconnect/keepalive (сегодня контракт реконнекта живёт только в потребителях — `MqttSink` и dashboard-луп реализуют его независимо).
