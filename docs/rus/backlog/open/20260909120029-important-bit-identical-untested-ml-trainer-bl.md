# «Re-run is bit-identical» не закреплён автотестом

- **Серьёзность**: важно
- **Компонент**: ml-trainer
- **Источник**: ревью `backlog/20260909120000-review.md`, приложение А5, «Важно» №3
- **Локация**: заявка — `ml/README.md:10, 73-79` («D6 double-run sha256 gate»); тесты — только компонентные (`writer_output_is_deterministic`, `float_format_roundtrip`, детерминизм split/calibration)

## Суть

Детерминизм полного пайплайна — ручная процедура (D6). Минорный релиз burn или рефакторинг молча сломает заявленное в README свойство.

## Предлагаемое исправление

Интеграционный тест в trainer:

- синтетический CSV (~40 окон);
- два прогона `pipeline::run` (2 эпохи, temp-директории);
- `assert_eq!` на `report.sha256` и байтах `.float`.

Быстро и закрывает README-заявление навсегда.
