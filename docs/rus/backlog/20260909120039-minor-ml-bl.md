# Мелочи: ml (exporter, trainer, features-cli)

- **Серьёзность**: мелочь
- **Компонент**: ml
- **Источник**: ревью `backlog/20260909120000-review.md`, приложение А5, «Мелочи» (№6-20)

Сводный список мелких замечаний:

1. `ml/exporter/Cargo.toml:11` — зависимость `csv` не используется нигде в crate. Убрать (offline-build не страдает).
2. `ml/trainer/src/pipeline.rs:235-238` — мёртвый `unused_path_assert` с `#[allow(unused)]`. Удалить.
3. `ml/trainer/src/train.rs:40, 73` — `let mut labels` никогда не мутируется; `let _ = &mut labels;` глушит warning вместо удаления `mut`.
4. `ml/exporter/src/quant.rs:313-330` — `#[derive(Default)]` у `Ranges` даёт `fc_min = 0.0` (несовместимо с `new()` = `±INFINITY`); будущий `Ranges::default()` молча даст неверные диапазоны. Реализовать `Default` вручную или убрать derive.
5. `ml/exporter/src/quant.rs:287-311` — `float_probs` гоняет forward дважды (`float_forward` + собственная цепочка). Извлечь цепочку или вернуть интермедиаты.
6. `ml/trainer/src/lib.rs:66-71` — `TaskSpec::name()` выводит идентичность из `num_classes == 4`; Q-спека с 4 классами логировалась бы как «a». Явный дискриминант задачи.
7. `features-cli/src/lib.rs:48-49` — док-пример протух: «128 samples … at 16 kHz (node Q) … 8 ms», фактически Q = 1024 @ 16 kHz = 64 ms; там же «provisional … to be pinned by the week-4 lab» — week-4 уже shipped (`model_q.tflite` закоммичен).
8. `ml/README.md:15` — флаг написан `--dataset`, реально `--datasets` (в L23 верно).
9. `ml/trainer/src/train.rs:66-71` — epoch loss = среднее батчевых средних без веса на размер: последний неполный батч весит как полный. Стоит комментария.
10. `ml/trainer/src/model.rs:163-174` — `windows_to_tensor` молча предполагает `CHANNELS == 1`; при будущем C>1 — тихая пересборка семплов. `debug_assert!` или комментарий.
11. `ml/trainer/src/data.rs:73-77` — `parse::<f32>()` принимает `"nan"`/`"inf"`: одно NaN-окно даёт NaN в `asymmetric_params` → scale NaN → `as i64 = 0`, мусорная модель молча. Отвергать non-finite при загрузке.
12. `ml/exporter/src/graph.rs:117-122` — все поля `ModelGraph` pub: валидацию GraphBuilder можно обойти, собрав структуру руками → `writer.rs:145` (`timesteps - pool`) может уйти в underflow. `pub(crate)` или валидировать в `write`.
13. `ml/exporter/tests/model_microflow.rs:51-52` — конвенция softmax (1/256, −128) захардкожена в тесте; при смене конвенции writer'а тест упадёт неочевидно. Читать из модели.
14. Нет числового parity-гейта interp↔microflow для **модели Q** (для A — ±2 кванта + float-parity). Риски низкие, но расширить `parity_gen`/тесты на Q — дёшево.
15. `ml/exporter/tests/writer_roundtrip.rs:94-104` — `committed_dummy_file_roundtrips` читает файл дважды и сравнивает с собой (тавтология). Gen_dummy полностью засеян — пересобрать dummy в тесте и сравнить байты с закоммиченным: настоящий freshness-гейт.
