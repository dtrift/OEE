# `--filter` не работает: префикс `oee/line1/` захардкожен

- **Серьёзность**: важно
- **Компонент**: oee-dashboard
- **Источник**: ревью `backlog/20260909120000-review.md`, приложение А3, «Важно» №3
- **Локация**: `oee-dashboard/src/state.rs:99` (strip_prefix), `main.rs:35-36` (CLI `--filter`)

## Суть

`topic.strip_prefix("oee/line1/")` жёстко, при этом CLI предлагает `--filter`. С `--filter oee/line2/#` подписка работает, но все сообщения отсекаются: UI молча пуст, ошибок нет.

## Предлагаемое исправление

- Выводить префикс из `--filter` (снять хвост `/#` или `...`) и передавать в `DashboardState`;
- либо убрать `--filter` из CLI.
