# Гейт недели 1 — декомпозиция (Spike)

> Оформлен 2026-08-25. Работа недели 1 завершена 2026-08-20 (коммит `b3d98bb`, PR #1 из
> `spike/foundation`). Чеклист — из [step-1.md](plan/step-1.md). При оформлении гейта
> проверки перепрогнаны: `cargo test` + `cargo clippy --all-targets -- -D warnings` в
> workspace и в форке — зелёные; `diff run1.csv run2.csv` (seed 42) — пустой.

## Чеклист гейта

| Пункт гейта                                           | Статус | Артефакт / проверка                                                                             |
| ----------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------- |
| `predict()` на хосте (`sine`); бонус — dense-модель   | да     | [NOTES.md](../../fork/NOTES.md) (Д1); [спайк-док](../../spike/conv1d-serialization.md), «Бонус» |
| Спека `Conv1D` достаточна для реализации без домыслов | да     | [conv1d-spec.md](../../fork/docs/conv1d-spec.md)                                                |
| Форк собирается, `cargo test` зелёный                 | да     | 25 lib + 3 integration; NOTES.md, Д1; перепроверено 2026-08-25                                  |
| Сериализация `Conv1D` задокументирована дампом ops    | да     | [conv1d-serialization.md](../../spike/conv1d-serialization.md)                                  |
| Каркас workspace собирается; детерминированный CSV    | да     | 22 теста + clippy; [determinism.rs](../../line-simulator/tests/determinism.rs)                  |

## Риски (разд. 11 плана)

- **№1 «Сборка MicroFlow на хосте» — не сбылся.** Форк собрался за вечер (~10 с release,
  тесты зелёные). Нюанс на неделю 3: git-patch `nalgebra` из манифеста форка не
  применяется через path-зависимость — секция `[patch.crates-io]` уже приготовлена в
  корневом `Cargo.toml` (закомментирована до появления зависимости).
- **№2 «Парсер TFLite сложнее ожидаемого» — сбылся.** Факт вместо `Reshape → CONV_2D`:
  `EXPAND_DIMS → CONV_2D → RESHAPE`, динамический Flatten (`SHAPE/STRIDED_SLICE/PACK`),
  per-channel веса FC и optional bias. Всё закрыто спекой (§2.1, §2.3, §3.3): неделя 2
  (кернел) не меняется, неделя 3 (парсер) объёмнее «понять Reshape-цепочку», но ограничена
  спекой.
- Остальные риски недели 1 не касались (модели, QEMU, тайминги — недели 4–6).

## Решение по неделе 2

По плану, перераспределение не требуется: кернел `conv_1d` по спеке §3, DoD — спека §6
(toy-тест + golden-кейсы, краевые случаи, clippy). Декомпозиция —
[step-2.md](plan/step-2.md).

## Ретро

- Оценка недели была ~16–19 ч; фактические часы по дням не журналировались — точного
  сравнения нет. С недели 2 — пометка о времени в конце каждой сессии.
- Спайк сделал своё: оба целевых риска сняты/обработаны до написания кернела.
- Сверх плана (параллельно, вне скоупа недели): колея железа — контракты `features-cli`,
  `SensorSource` в `nodes`, скелет `firmware/` (PR #2); CI на оба workspace.
- Мелочи на будущее: TF требует Python 3.12 (venv в `tmp/`); конвертер выбрасывает нулевой
  bias FC; `target/microflow-expansion.rs` — быстрый дебаг кодегена.
