# Сценарии soak: 100 тысяч и 1 миллион сообщений через стенд

> [`scenarios/soak.toml`](../../scenarios/soak.toml) — сценарий под
> нагрузку сообщениями (неделя 6): те же плотности, что в
> [`scenarios/week5/normal.toml`](../../scenarios/week5/normal.toml),
> растянутые до 3 часов симулированного времени (~100 000 сообщений).
> [`scenarios/soak-1m.toml`](../../scenarios/soak-1m.toml) — ~12 часов с
> периодами ленты/тапов 150 мс: ~1 072 500 сообщений (~2.2 ГБ артефактов —
> следите за диском). Это soak-тесты стендового брокера и агрегатора, а
> не демо-сценарии. Все числа ниже — из проверенных прогонов (сид 42).

## Что ожидать (измерено, сид 42)

| Метрика                                        | soak.toml (3 ч)                              | soak-1m.toml (12 ч, 150 мс)                   |
| ---------------------------------------------- | --------------------------------------------- | --------------------------------------------- |
| сообщений на `oee/line1/#` (счётчик дашборда)  | 99 787                                        | 1 072 502                                     |
| публикаций узлов A / P / Q                     | 9 / 24 241 / 25 465                           | 24 / 260 876 / 274 634                        |
| агрегатор                                      | 49 712 принял, 50 072 переиздал, 0 ошибок     | 535 534 принял, 536 968 переиздал, 0 ошибок    |
| минутных окон                                  | 180 (+ shift-скоп; 361 строка CSV)           | 717 (+ shift-скоп)                            |
| статусных строк узла A (контроль флапа)        | 7                                             | 23                                            |
| OEE                                            | 0.853 (A 0.943, P 0.952, Q 0.950), 24 239 деталей | 0.906 (A 0.954, P 1.000*, Q 0.950), 260 875 деталей |
| генерация потоков                              | 25 с                                          | ~35 с                                         |
| весь реплей, release-бинарники                 | 13 с                                          | ~1.5–2 мин                                    |
| артефакты                                      | ~585 МБ (`run.csv` ~337 МБ, `taps.csv` ~248 МБ) | ~2.2 ГБ (`run.csv` ~1.35 ГБ, `taps.csv` ~0.99 ГБ) |

\* P зажимается в 1.000: идеальный цикл стенд-агрегатора остаётся 400 мс
(`scripts/bench.sh`), а детали приходят каждые ~143 мс — для осмысленного
P запустите агрегатор с `--ideal-cycle-ms 150`. A и Q остаются
осмысленными без этого.

## Стена 30 часов (почему soak-1m — это 12 часов, а не 30)

Наивное масштабирование до миллиона — 30 часов на дефолтных плотностях —
было собрано и прогнано, и оно честно сломалось: фаза несущей в
симуляторе считается в f32-секундах (`sin(2π·f·t)`,
`line-simulator/src/signal.rs`), и её погрешность растёт с t. Измерено на
30-часовом прогоне: узел A флапал 2–6 раз в час до 13-го часа, а с 14-го —
600–4 800 раз в час (36 523 статусных строки; измеренный Availability
рухнул до 0.591 при правде ~0.92 — классификатор читал шум фазы, а не
огибающую). Двенадцать часов остаются внутри чистого окна; недостающий
объём сообщений добирается плотностью (периоды ленты/тапов 400 → 150 мс).
Правильный фикс — накопление фазы в f64 — изменил бы генерируемый сигнал
бит-в-бит и обесценил обученные модели и запись недели 5, поэтому он
закреплён как будущая работа, а не изменение недели 6.

События состояний станка раскинуты по всей шкале намеренно: узел A
публикует только при подтверждённой смене статуса, и сгруппированные
события заставили бы его молчать часами (P и Q стримят независимо — они и
доминируют в счёте).

## Запуск 1: одной командой

```bash
RELEASE=1 scripts/bench.sh scenarios/soak.toml 42    # release: реплей ~13 с
```

Скрипт сгенерирует потоки (~585 МБ в `tmp/bench/`), поднимет брокера и
агрегатор, откроет дашборд и запустит узлы в фоне. Счётчик сообщений
намотает ~99 800, затем «stream ended»; гейджи замрут на 3-часовых
значениях (OEE 85.3%, A 94.3%, P 95.2%, Q 95.0%, деталей 24 239). Выход —
`q`.

Без `RELEASE=1` скрипт собирает **debug**-бинарники (правильный компромисс
в демо-масштабе 60 с) — и на таком CSV узлы идут последовательно: ~3
минуты тишины узла A, потом burst от P и ещё ~10–15 минут узла Q. Дашборд
при этом НЕ завис — он ждёт между редкими debug-публикациями.

## Запуск 2: быстро (release, реплей ~13 с)

```bash
cargo build -q --release -p mqtt-min -p nodes -p oee-aggregator -p oee-dashboard

PORT=18845; ADDR=127.0.0.1:$PORT; OUT=tmp/soak
./target/release/broker $PORT >"$OUT/broker.log" 2>&1 &
./target/release/aggregator --mqtt $ADDR --ideal-cycle-ms 400 \
    --out "$OUT/oee_windows.csv" >"$OUT/aggregator.log" 2>&1 &
sleep 0.5
( sleep 1
  ./target/release/node --kind a --input "$OUT/run.csv"   --offline "$OUT/statuses.csv" --mqtt $ADDR --run-id soak-42 >"$OUT/a.log" 2>&1
  ./target/release/node --kind p --input "$OUT/ir.csv"    --offline "$OUT/counts.csv"   --mqtt $ADDR --run-id soak-42 >"$OUT/p.log" 2>&1
  ./target/release/node --kind q --input "$OUT/taps.csv"  --meta "$OUT/taps_meta.csv" \
      --offline "$OUT/verdicts.csv" --mqtt $ADDR --run-id soak-42 >"$OUT/q.log" 2>&1
) &
./target/release/oee-dashboard --mqtt $ADDR
```

Секунда форы (`sleep 1`) даёт дашборду подписаться до первой публикации
(у брокера нет ретенций). После `q` на дашборде остановите фоновые job'ы —
и проверьте осиротевших брокеров, если этот шаг пропущен или терминал
закрылся раньше (они держат порт, но иначе простаивают безвредно):

```bash
kill %1 %2 2>/dev/null
pgrep -af 'target/release/broker' || echo "осиротевших брокеров нет"
```

Для полностью чистого прогона сперва перегенерируйте потоки (release-
симулятор — считанные секунды):

```bash
mkdir -p tmp/soak
./target/release/line-simulator --scenario scenarios/soak.toml --seed 42 \
    --out tmp/soak/run.csv --taps-dataset tmp/soak/taps.csv --taps-meta tmp/soak/taps_meta.csv \
    --belt-events tmp/soak/ir.csv --belt-meta tmp/soak/belt_meta.csv
```

## Уборка

Артефакты soak весят ~585 МБ на выходной каталог:

```bash
rm -rf tmp/soak tmp/bench
```

## Почему debug здесь медленный

Узел A парсит 337 МБ CSV из 135 000 окон: 184 с в debug против 6 с в
release — сам инференс пренебрежим (135 000 × ~15 мкс ≈ 2 с). В
демо-масштабе (60-секундные сценарии) debug — правильный компромисс; в
soak-масштабе используйте `RELEASE=1` (запуск 1) или release-блок
(запуск 2).
