[package]
name = "firmware-p"
description = "Node P firmware (Performance): TCRT5000 -> edge + 50 ms debounce -> counting"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# Uncomment for the first on-board build (see ../README.md):
# board = { path = "../board" }
# esp-hal = { version = "1.1.2", features = ["esp32s3"] }
