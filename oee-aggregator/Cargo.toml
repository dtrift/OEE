[package]
name = "oee-aggregator"
description = "MQTT subscription, OEE = A x P x Q formula, publishing for the dashboard (week 5)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
