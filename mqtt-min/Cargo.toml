[package]
name = "mqtt-min"
description = "A minimal MQTT 3.1.1 client subset (CONNECT/PUBLISH QoS0/SUBSCRIBE QoS0/PINGREQ) over std TCP, plus a loopback broker and a bench broker binary"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
