[package]
name = "mqtt-min"
description = "A minimal MQTT 3.1.1 client subset (CONNECT/PUBLISH QoS0/PINGREQ) over std TCP — the offline answer to the week-4 MQTT step"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
